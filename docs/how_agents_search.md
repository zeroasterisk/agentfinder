# How Agents Search

> 📝 **Status**: Stub Page (Assigned to **Guha**)

This guide will outline how AI client orchestrators (such as Claude Code, Gemini, Copilot) dynamically query federated search endpoints at runtime.

---

## Assigned Action Items (Guha):
*   [ ] Document the `POST /search` payload schema and response parsing.
*   [ ] Explain how to handle `auto` vs. `referrals` federation modes in clients.
*   [ ] Provide client-side trust verification steps using the `trustManifest` layer.
